import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-codelab',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
